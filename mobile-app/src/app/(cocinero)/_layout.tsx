import { Stack } from "expo-router";

/** Grupo de rutas del cocinero. Acá vivirán las pantallas propias de cocina. */
export default function CocineroLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
